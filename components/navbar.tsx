export default function NavbarPage({ type }: { type: string }) {
  return (
    <main className="w-full bg-[#00695C] flex justify-between align-center shadow-md px-5 py-3 text-white">
      <img src="/img/fulllogo.png" alt="logo" className="h-8" />
    </main>
  );
}
